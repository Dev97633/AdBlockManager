pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
    repositories {
        google()
        mavenCentral()

        // Main Rikka repo (for shizuku + hidden api)
        maven { url "https://maven.rikka.app" }

        // Optional mirror
        maven { url "https://storage.googleapis.com/rikka-repo" }
    }
}

rootProject.name = "AdBlockManager"
include(":app")
